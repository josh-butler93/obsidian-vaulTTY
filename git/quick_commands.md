git add . # whole repo 
git add <folder_name> # adding a folder
git add file_name.md # adding a single file 

git commit -m "Comments...for commit"

git push origin <branch_name>

and your done

git pull origin branch_name #to pull down any updated changes
