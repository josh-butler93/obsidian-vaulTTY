-----------------
--curl -fsSL https://herdr.dev/install.sh | sh

--------------------------------
-echo 'export PATH="$HOME/.local/bin:$PATH"' >> ~/.bashrc
------or-------
--echo 'export PATH="$HOME/.local/bin:$PATH"' >> ~/.zshrc
---source ~/.bashrc
----source ~/.zshrc
-----herdr --version
------herdr ==run the application

--------------------------------
-ctrl + b + q ==exits out of the termina
--ctrl + b + s ==settings
---herdr server stop
----herdr session list
----herdr workspace list
-----herdr --remote <remote-session> ==this command allows you to connect to an ssh session attaching where you are from to that sessions herdr || start the session on the remote host
