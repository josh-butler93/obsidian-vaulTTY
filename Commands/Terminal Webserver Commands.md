commadn: npx -v
command: npm -v
- if neither is installed install them
- naviagate to the directory that you want to build the test webpage in and run the below command
- command:
	- npx serve .
		- select 'y'
		- go to the site it is showing
	-  npx serve . &
		- the above command runs the server in the background
- command:
	- ctrl + c to end the session

Vite
command: npm init vite@latest
	vanilla
		javascript 
command: npm run dev
	go to the address it has posted